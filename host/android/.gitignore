.gradle/
build/
local.properties
.idea/
*.iml
captures/
.externalNativeBuild/
.cxx/

# El bundle de Lynx es un artefacto de build: `copyLynxBundle` lo deja aqui
# desde dist/ (la raiz del repo) y no se versiona.
app/src/main/assets/*.bundle
